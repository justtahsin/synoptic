[package]
name = "synoptic-core"
version = "0.1.0"
edition = "2021"
description = "UI-independent Linux system data collection layer for Synoptic"
license = "MIT OR Apache-2.0"

[dependencies]
libc = "0.2"
