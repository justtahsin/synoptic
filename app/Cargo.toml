[package]
name = "synoptic-app"
version = "0.1.0"
edition = "2021"
description = "Synoptic - the familiar task manager for Linux"
license = "GPL-3.0-or-later"

[[bin]]
name = "synoptic"
path = "src/main.rs"

[dependencies]
slint = "1"
i-slint-backend-winit = "=1.17.1"
synoptic-core = { path = "../core" }

[build-dependencies]
slint-build = "1"
